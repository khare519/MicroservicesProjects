1\) **mvn -version** : To check the version of the maven installed.

2\) **mvn clean install** : To build the project and create a fat jar of specific microservice in target folder. It will store it in local maven repo.

3\) **java -jar target/<fat-jar name>** : To run that jar file.

